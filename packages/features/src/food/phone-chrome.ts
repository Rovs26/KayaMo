/** Phone chrome. Desktop passes a different class so we never center a 390px column. */
export const PHONE_TOAST_DOCK_CLASS =
  'fixed inset-x-0 bottom-[5.5rem] z-40 mx-auto w-full max-w-[390px] px-4';
